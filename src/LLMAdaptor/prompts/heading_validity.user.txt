Validate the following heading candidates.

HARD REQUIREMENTS (must follow exactly):
- Return JSON only (no markdown fences, no prose).
- Return an object with exactly this shape:
  {"results":[{"id":string,"is_valid":boolean,"reason":string}]}
- The "results" array MUST have the same length and order as the input array.
- Each result.id MUST EXACTLY match the corresponding input item's "id" (copy it verbatim).
- Do NOT invent new ids. Do NOT add extra items. Do NOT drop items.

Input JSON (array):
{candidates_json}
